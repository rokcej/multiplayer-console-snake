# Multiplayer Snake
Multiplayer snake game written in C

* Installing dependencies:
	```bash
	sudo apt-get install libncurses5-dev
	```
* Compiling the code:
	```bash
	make
	```
* Running the program:
	```bash
	build/server
	build/client
	```
